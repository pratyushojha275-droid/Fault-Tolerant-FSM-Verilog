# Fault-Tolerant-FSM-Verilog
A normal fault-tolerant-FSM do not guarantee state transition flow maintain, hence I use Hamming coded state encoding of states hence error cannot exceed more than 1 bit and if 1 bit flip then it must be 1 hamming distance from previous valid state, not any other valid state.
